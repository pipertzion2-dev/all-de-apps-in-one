{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm install && cd Svivva && npm ci",
  "buildCommand": "npm run vercel-build",
  "crons": [
    {
      "path": "/api/cron/run-scheduled?job=seo",
      "schedule": "0 6 * * *"
    },
    {
      "path": "/api/cron/run-scheduled?job=growth",
      "schedule": "0 7 * * 0"
    }
  ]
}
