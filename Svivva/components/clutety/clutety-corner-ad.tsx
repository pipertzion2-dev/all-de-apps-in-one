"use client";

/**
 * Clutety promo is intentionally disabled until the product is ready.
 * Keep this stub so accidental re-imports render nothing.
 */
export function ClutetyCornerAd() {
  return null;
}
