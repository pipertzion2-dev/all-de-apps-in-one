"use client";

import { ZzaiModeToggle } from "@/components/zzai-mode-toggle";

/** @deprecated Prefer ZzaiModeToggle — kept as a thin alias for older imports. */
export function PlatformToggle() {
  return <ZzaiModeToggle size="sm" showLabels />;
}
